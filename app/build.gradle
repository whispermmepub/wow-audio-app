plugins {
    id 'com.android.application'
}

android {
    namespace 'com.whisper.wowaudio'
    compileSdk 36

    defaultConfig {
        applicationId 'com.whisper.wowaudio'
        minSdk 24
        targetSdk 36
        versionCode 15
        versionName '1.9.0'

        ndk {
            abiFilters 'arm64-v8a', 'armeabi-v7a'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    androidResources {
        noCompress += ['onnx']
    }

    buildTypes {
        debug {
            // Keep the F5 field-test build installable beside the user's permanently-signed WoW Audio.
            // Release builds keep the real com.whisper.wowaudio package unchanged.
            applicationIdSuffix '.f5test'
        }
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    lint {
        abortOnError true
        checkReleaseBuilds true
    }
}

dependencies {
    implementation 'androidx.core:core:1.16.0'
    implementation 'org.jetbrains.kotlin:kotlin-stdlib:1.7.20'
    implementation 'com.squareup.okhttp3:okhttp:4.12.0'
    implementation files('libs/sherpa-onnx-1.13.7.aar')
    // One shared ONNX Runtime for both Sherpa offline backup and F5 custom voice.
    implementation 'com.microsoft.onnxruntime:onnxruntime-android:1.29.0'
    testImplementation 'junit:junit:4.13.2'
}
